<h1 class="jumbotron text-center mb-5 mt-5">Modifier le potin n°<%=@gossip.id%></h1>

<%= form_for @gossip, :html => {:class => "mt-5 form-signin text-center mx-auto user_form mb-5"} do |f|%>

  <div class="form-group text-left">
  <label>Titre</label>
    <%= f.text_field :title, class:"form-control", placeholder:"Titre" %>
  </div>

  <div class="form-group text-left">
    <label>Potin</label>
    <%= f.text_area :content, class:"form-control", placeholder:"Potin"%>
  </div>

  <div class="form-group">
    <button type="submit" class="btn btn-lg btn-primary btn-block">Modifier le gossip</button>
  </div>

  <%= hidden_field_tag "gossip[user_id]", @user_id %>
  <% end %>